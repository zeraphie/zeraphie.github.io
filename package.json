{
  "name": "izelya.me",
  "version": "0.0.1",
  "private": true,
  "description": "Izzy's personal site — the hp-grid world.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "typecheck": "astro check",
    "test": "bun test --pass-with-no-tests",
    "lint": "oxlint src/",
    "lint:fix": "oxlint --fix src/",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "check": "bun run format:check && bun run lint && bun run typecheck && bun run test && bun run build"
  },
  "dependencies": {
    "@hexpunk/core": "github:zeraphie/hexpunk#v0.3.2",
    "astro": "^7.2.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "typescript": "^6.0.3"
  },
  "trustedDependencies": [
    "@hexpunk/core"
  ]
}
