{
  "name": "izelya.me",
  "version": "0.0.1",
  "private": true,
  "description": "Izzy's personal site — the hp-grid world.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "demo": "cloudflared tunnel --url http://localhost:4321",
    "typecheck": "astro check",
    "test": "bun test",
    "lint": "oxlint src/",
    "lint:fix": "oxlint --fix src/",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "check": "bun run format:check && bun run lint && bun run typecheck && bun run test && bun run build"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.4",
    "@astrojs/sitemap": "^3.7.3",
    "@hexpunk/core": "github:zeraphie/hexpunk#v0.3.4",
    "astro": "^7.2.7"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "typescript": "^6.0.3"
  },
  "trustedDependencies": [
    "@hexpunk/core"
  ]
}
