# dependencies + build output
node_modules/
dist/
.astro/

# local-only plans (the hexpunk house pattern)
.plan/

# env
.env
.env.*

# local assistant tooling — the committed AI layer is .ai/
.claude/
